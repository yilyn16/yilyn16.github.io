import type { App, ThemeApi } from '../types';
/**
 * Create vuepress theme-api
 *
 * Resolve current theme, parent theme, theme layouts
 */
export declare const createThemeApi: (app: App) => Promise<ThemeApi>;
