import type { App } from '../types';
/**
 * Resolve page data file path
 */
export declare const resolvePageDataInfo: ({ app, key, }: {
    app: App;
    key: string;
}) => {
    dataFilePath: string;
    dataFilePathRelative: string;
    dataFileChunkName: string;
};
