"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePagesData = void 0;
/**
 * Generate page path to page data map temp file
 */
const preparePagesData = async (app) => {
    // generate page data map
    const content = `\
export const pagesData = {
${app.pages
        .map(({ path, dataFilePath, dataFileChunkName }) => `\
  ${JSON.stringify(path)}: () => import(${dataFileChunkName ? `/* webpackChunkName: "${dataFileChunkName}" */` : ''}${JSON.stringify(dataFilePath)}).then(({ data }) => data),`)
        .join('\n')}
}
`;
    await app.writeTemp('internal/pagesData.js', content);
};
exports.preparePagesData = preparePagesData;
