"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePagesComponent = void 0;
/**
 * Generate page key to page component map temp file
 */
const preparePagesComponent = async (app) => {
    // generate page component map file
    const content = `\
import { defineAsyncComponent } from 'vue'

export const pagesComponent = {\
${app.pages
        .map(({ path, componentFilePath, componentFileChunkName }) => `
  ${JSON.stringify(path)}: defineAsyncComponent(() => import(${componentFileChunkName
        ? `/* webpackChunkName: "${componentFileChunkName}" */`
        : ''}${JSON.stringify(componentFilePath)})),`)
        .join('')}
}
`;
    await app.writeTemp('internal/pagesComponent.js', content);
};
exports.preparePagesComponent = preparePagesComponent;
