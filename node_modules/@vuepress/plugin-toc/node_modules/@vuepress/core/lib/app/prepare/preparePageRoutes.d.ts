import type { App, Page } from '../../types';
/**
 * Generate page routes temp file of a single page
 */
export declare const preparePageRoutes: (app: App, { key, path, pathInferred, filePathRelative, title, routesFilePathRelative, }: Page) => Promise<void>;
